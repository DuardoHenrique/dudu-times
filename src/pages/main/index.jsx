import { SectionMain } from "../../components/main/section"
import {Container} from '../../components/main/container'

export const Main = () => {
  return (
    <SectionMain>
      <Container />
    </SectionMain>
  )
}