import { styled } from "styled-components";

export const ContainerMain = styled.section`
  display: grid;
  grid: 'left center right ' / 1fr minmax(200px, 3fr) 1fr;

  min-height: 90vh;
  width: 100%;
`